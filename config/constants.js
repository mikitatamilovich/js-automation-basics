/**
 * Status value that marks an item as available in stock.
 * @type {string}
 */
export const STATUS_IN_STOCK = "in_stock";

/**
 * Default number of retries used when the raw config does not provide one.
 * @type {number}
 */
export const DEFAULT_RETRIES = 3;

/**
 * Fallback auth token used when no token is present in the raw config.
 * @type {string}
 */
export const DEFAULT_AUTH_TOKEN = "guest_token";
